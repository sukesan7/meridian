# 3A Backtester (Strategy 3A — VWAP Trend Pullback)

Deterministic backtester for NQ/ES during US RTH. Python 3.10+, pandas-first.

## Install
```bash
python -m venv .venv && source .venv/bin/activate
pip install -e .[dev]
pre-commit install
