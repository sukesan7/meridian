# Normalize to LF in repo
* text=auto eol=lf

# Keep Windows scripts CRLF locally
*.ps1 eol=crlf
*.bat eol=crlf

# Treat binaries as binary (no line-ending munging)
*.parquet -text
*.png -text
*.jpg -text
*.gif -text
*.zip -text
*.ico -text
*.ipynb linguist-vendored
